import os
from config.contract import *

base_path = os.path.join(os.environ['BASE_PATH'], os.environ['RECORD_PATH'])
tensorboard_path = os.path.join(os.environ['BASE_PATH'], os.environ['TENSORBOARD_PATH']) 
