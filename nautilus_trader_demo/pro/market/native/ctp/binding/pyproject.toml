[project]
name = "bomber-ctp"
version = "0.1.1"
description = "Bomber project-owned CTP market-data and trader bindings"
requires-python = ">=3.10"

[build-system]
requires = ["meson-python>=0.17.1", "meson>=1.7.0", "pybind11>=2.13.6"]
build-backend = "mesonpy"
